class Student:
    def __init__(self, name, cohort):
        self.name = name
        self.cohort = cohort

    def student_has_name(self, name):
        return name

    def student_has_cohort(self, cohort):
        return cohort
